"""DeskPet version constants.

`APP_VERSION` is the single application-version source of truth. Version tags use
`v{APP_VERSION}`; GitHub Releases are published separately according to VERSIONING.md.
"""
APP_NAME = "DeskPet"
APP_VERSION = "4.1.0"
APP_LABEL = f"{APP_NAME} V{APP_VERSION}"
