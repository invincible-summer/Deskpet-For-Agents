# 4.5.0 验收记录

本版聚焦进程监听状态解析，保留并完善工作区已有修改。

- 全量 `unittest discover`：774 项通过。
- 状态、融合、摘要、桌面来源、呈现和版本专项：194 项通过（包含新增回归）。
- 最终命令包装解析改动后：93 项状态、监听和摘要回归通过。
- 监听压力测试：5000 ticks，全部边界检查通过。
- 桌面来源压力测试：5000 ticks，查询预算、会话上限、线程/句柄及数据库繁忙恢复全部通过。
- 呈现与 UI 架构基准：全部检查通过。
- Windows 150% DPI 真实窗口渲染：21 项自动检查通过；人工查看了外观紧凑布局。Agent 长详情截图未呈现 fixture Agent，因此不作为长文本实际布局通过的证据；文本长度和展示模式由自动回归覆盖。
- Python 编译与 `git diff --check` 通过。

新增回归覆盖：长时间无日志仍工作、错误不复活、新用户输入清旧状态、同步/异步问题生命周期、Claude 匹配问题结果与结束事件、Goal 双显示模式、命令语法与长度、JavaScript 包装中的字面命令提取。

限制：使用协议 fixture 验证 Agent 状态转换，未要求用户操作所有 Agent 的真实审批界面。无结构化或终端审批证据时不推断审批；完成提示窗口后转为等待新任务。

CI 修复了约束文件路径，接入此前未被 unittest discovery 收集的函数式状态回归，并加入桌面来源基准运行步骤。
