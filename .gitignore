# ---- 素材版权保护：桌宠素材不入库，用户自行放置 ----
assets/pets/*
!assets/pets/README.md
assets/cache/
assets/icon.ico

# ---- 本机配置（含个人路径；V4.1 起含备份与写盘临时文件） ----
config.json
config.json.bak
.deskpet-config-*

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Local UI snapshots and measurements
.test-artifacts/

.zcode/
# 本地 benchmark 运行产物（CI 在 runner 上生成并以 artifact 上传）
benchmark-report.json
